// Top-level build file
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
    // Si usas el plugin de serialization a nivel módulo, no necesitas declararlo aquí
}
